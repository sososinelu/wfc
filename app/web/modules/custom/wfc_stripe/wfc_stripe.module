<?php

/**
 * @file
 * Module file for the Wanderers' Flight Club stripe module, contains all the main hooks and logic functions.
 */

use Drupal\user\Entity\User;

/**
 * Implements hook_theme().
 * @param $existing
 * @param $type
 * @param $theme
 * @param $path
 * @return array
 */
function wfc_stripe_theme($existing, $type, $theme, $path)
{
  return [
    'stripe_test_form' => array(
      'variables' => array('vars' => NULL),
      'template' => 'stripe-test-form'
    ),
  ];
}
